<%= turbo_stream.prepend 'feed-posts', partial: 'post', locals: { post: @post } %>

<%= turbo_stream.replace 'new_post_form', partial: 'new_post_form' %>

<%= turbo_stream.update 'flash', partial: 'flash' %>
