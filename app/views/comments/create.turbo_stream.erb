<%= turbo_stream.prepend dom_id(@comment.post, 'comments'), @comment %>

<%= turbo_stream.replace dom_id(@comment.post, 'comment_form'), partial: 'post_comment_new', locals: { post: @comment.post } %>

<%= turbo_stream.update dom_id(@comment.post, 'comments_toggle') do %>
  <%= pluralize(@comment.post.comments.length, 'Comment') %>
<% end %>
