<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<ul>
  <% @users.each do |user| %>
    <li><%= user.email %><%= button_to 'Send Friend Request', friend_requests_path, params: { user: } %></li>
  <% end %>
</ul>
